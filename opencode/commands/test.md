---
description: Run tests via the built-in shell tool
agent: build
---

Run the test suite for $ARGUMENTS using the project's normal test command
(check AGENTS.md / package.json / pyproject.toml if unsure) and summarize
any failures.
